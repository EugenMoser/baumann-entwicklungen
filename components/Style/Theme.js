import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      paper: "#fff444",
    },
    primary: {
      main: "#ffc0cb",
    },
  },
});
export default theme;
